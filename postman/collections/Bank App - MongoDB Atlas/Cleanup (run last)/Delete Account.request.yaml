$kind: http-request
url: "{{baseUrl}}/api/accounts/{{accountId}}/delete"
method: POST
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status code is 200', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 1000
